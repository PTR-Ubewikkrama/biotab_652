version: "3.9"

services:
  # MySQL
  db:
    container_name: test-jig-mysql-db
    image: mysql:8.0
    restart: no
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: test_jig
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    ports:
      - '3306:3306'