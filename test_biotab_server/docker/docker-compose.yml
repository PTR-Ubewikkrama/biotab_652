version: "3.9"

services:
  # MySQL
  db:
    container_name: biotab-e652-mysql-db
    image: mysql:8.0
    restart: no
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: biotab_e652
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    ports:
      - '3306:3306'